services:
  api:
    volumes:
      - ${AWS_SHARED_CREDENTIALS_DIR:?Set AWS_SHARED_CREDENTIALS_DIR to your host ~/.aws path}:/home/node/.aws:ro

  worker:
    volumes:
      - ${AWS_SHARED_CREDENTIALS_DIR:?Set AWS_SHARED_CREDENTIALS_DIR to your host ~/.aws path}:/home/node/.aws:ro
